@model IEnumerable<Team>

@{
    ViewBag.Title = "Teams";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-9">
        <h2>Teams</h2>
        <ol class="breadcrumb">
            <li>
                <a href="@Url.Action("Index", "Team")">Team</a>
            </li>
            <li class="active">
                <strong0>Index</strong0>
            </li>
        </ol>
    </div>
</div>

<div class="wrapper wrapper-content animated fadeInRight">

    @if(Model.Count() > 0)
    {
        foreach (var team in Model)
        {
            <div class="col-lg-4">

                <div class="widget p-xl lazur-bg" style="background-color:@team.Color;cursor:pointer;" onclick="toDetails(@team.ID)">
                    <h2 class="font-bold">
                        @team.Name
                    </h2>
                    <ul class="list-unstyled m-t-md">
                        <li>
                            <span class="fa fa-code m-r-xs"></span>
                            <label>Code:</label>
                            @team.Code
                        </li>
                        <li>
                            <span class="fa fa-bookmark m-r-xs"></span>
                            <label>Description:</label>
                            @team.Description
                        </li>
                        <li>
                            <span class="fa fa-tasks m-r-xs"></span>
                            <label>Team Leader:</label>
                            @team.TeamLeader.FullName
                        </li>
                    </ul>
                </div>
            </div>
        }
    }
    else
    {
        <div class="widget yellow-bg p-lg text-center" style="cursor:pointer" onclick="toCreate()">
            <div class="m-b-md">
                <i class="fa fa-asterisk fa-4x"></i>
                <h3 class="font-bold no-margins">
                    You don't have any teams yet.
                </h3>
                <small>Click here to create.</small>
            </div>
        </div>
    }
</div>

<script type="text/javascript">

    function toDetails(id)
    {
        window.location = "@Url.Action("Details")" + "/" + id;
    }

    function toCreate()
    {
        window.location = "@Url.Action("Create")";
    }
</script>